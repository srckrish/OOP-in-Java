# Hello World

## What I learned

- Every Java program needs a `class`
- Execution starts from `main` method
- `System.out.println()` prints to console
- Java files must be saved as `ClassName.java`

## How to run

1. Compile: `javac HelloWorld.java`
2. Run: `java HelloWorld`
